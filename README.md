# conversion
A collection of experimental conversion manipulation functions for Flux code blocks. These are candidates for addition to the Flux block library. 

### `numToString(num)`
Coverts a number to string or list of numbers to list of strings.
